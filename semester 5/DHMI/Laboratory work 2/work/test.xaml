<Window x:Class="work.test" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:work.Properties"
        Title="{x:Static res:Resources.Window}" 
        Height="165" 
        Width="260" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="ToolWindow" 
        ResizeMode="NoResize">
    <Grid>
        <Label Content="{x:Static res:Resources.MainLabel}" Margin="10,0,10,0" VerticalAlignment="Top" FontStyle="Italic" HorizontalAlignment="Center"/>
        <Label Content="{x:Static res:Resources.Name}" HorizontalAlignment="Left" Margin="10,26,0,0" VerticalAlignment="Top"/>
        <Label Content="{x:Static res:Resources.Pass}" HorizontalAlignment="Left" Margin="10,57,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="tLogin" Height="23" Margin="122,28,10,0" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <PasswordBox x:Name="pPassword" Margin="122,59,10,0" VerticalAlignment="Top" Height="23"/>
        <Button x:Name="bEnter" Content="{x:Static res:Resources.Enter}" Margin="0,0,10,13" Height="22" VerticalAlignment="Bottom" Grid.RowSpan="2" HorizontalAlignment="Right" Width="75" Click="bEnter_Click"/>
    </Grid>
</Window>
