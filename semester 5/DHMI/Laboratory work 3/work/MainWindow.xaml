<Window x:Class="work.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:work"
        mc:Ignorable="d"
        Title="Студенты" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="table">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Path=Surname}"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Path=Patronymic}"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding Path=Birth}"/>
                <DataGridTextColumn Header="Факультут" Binding="{Binding Path=Faculty}"/>
                <DataGridTextColumn Header="Группа и пр." Binding="{Binding Path=Group}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
